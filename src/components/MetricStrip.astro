---
import type { Metric } from "../data/projects";

type Props = {
  metrics: Metric[];
};

const { metrics } = Astro.props;
---

<dl class="metric-strip">
  {metrics.map((metric) => (
    <div>
      <dt>{metric.label}</dt>
      <dd>{metric.value}</dd>
    </div>
  ))}
</dl>
